[pytest]
markers =
    fast: Fast tests using ACU_MODE=light (no real ML/Unity)
    full: Full E2E tests using ACU_MODE=full (real services required)
    e2e: End-to-end pipeline tests
    unit: Unit tests with mocked services
